name_model = "70M"
project = "70m_prime_simple"
type_model = "llama2"

[train]
micro_bs = 64 # change this base on the gpu
reshard_after_forward = false

[optim]
batch_size = 512
warmup_steps = 500
total_steps = 4000

[optim.optim]
lr = 1e-3

